To build, run
    zsh build.sh

Wakeup.app will be created in the same directory. Xcode is NOT needed if you
have Command Line Tools installed.

Running it will create an icon on the menu bar that can let you disable sleep
when closing the lid. Enjoy!

-- Andy Zhang
